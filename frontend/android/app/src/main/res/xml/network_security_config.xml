<?xml version="1.0" encoding="utf-8"?>
<!--
  Release builds only talk HTTPS (Railway). Cleartext HTTP is allowed
  exclusively for local development hosts (emulator loopback). For a
  physical device against a LAN backend, debug builds permit all
  cleartext via the debug manifest overlay.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
